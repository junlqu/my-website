export const metadata = {
  title: "Junlin Qu | Resume",
  description: "Junlin Qu's resume.",
}

export default function ResumeLayout({ children }) {
  return (
    <>
      {children}
    </>
  );
}
